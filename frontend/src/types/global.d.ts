interface UserProps {
  firstName: string;
  lastName: string;
  email: string;
  userId: string;
}

interface MetadataProps {
  image?: string;
  icons?: string;
  description?: string;
  title?: string;
  noIndex?: boolean;
}
